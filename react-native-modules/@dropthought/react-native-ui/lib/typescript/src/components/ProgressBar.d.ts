import * as React from 'react';
/**
 * The ProgressBar will use `value` and `maxValue` to
 * compute the percentage
 */
type Props = {
    value: number;
    maxValue: number;
    themeColor: string;
    color: string;
    rtl: boolean;
};
declare const _default: React.MemoExoticComponent<({ value, maxValue, themeColor, color, rtl }: Props) => React.JSX.Element>;
export default _default;
//# sourceMappingURL=ProgressBar.d.ts.map